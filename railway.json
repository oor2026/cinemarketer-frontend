{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Static site - no build needed'"
  },
  "deploy": {
    "startCommand": "npx serve -s src -l 3000",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}