{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Static site - no build needed'"
  },
  "deploy": {
    "startCommand": "python -m http.server 3000 --directory src",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}